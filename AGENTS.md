# Kaggle Study Project

## 1. Goal
- 이 프로젝트에서는 총 5개의 Kaggle 대회를 순서대로 학습한다.
- 목표는 높은 Leaderboard 점수 자체보다 데이터 분석, EDA, validation,
  feature engineering, 모델링의 전체 과정을 학습하는 것이다.
- Codex는 사용자의 학습을 보조한다. 중요한 분석이나 모델링 판단을
  임의로 전부 진행하지 않는다.

## 2. Analysis Report
- 데이터 분석 결과, 표, 그래프, 모델 성능, 실험 결과 등
  사용자가 확인해야 할 내용은 GitHub Pages용 HTML 리포트에 정리한다.
- 중요한 분석이나 실험이 추가될 때마다 HTML 리포트도 갱신한다.
- HTML은 PC와 모바일에서 보기 편하게 구성한다.
- 모든 중간 결과를 넣지 말고, 분석 및 의사결정에 필요한 결과를 우선한다.

## 3. Data
- Kaggle 원본 데이터는 로컬 `data/` 폴더에 저장한다.
- `data/`는 `.gitignore`에 포함하고 GitHub에 업로드하지 않는다.